• Built an Assembler that converted .asm files to .out files.
• Simulated a single cycle processor which was later converted into 5-stage piplined processor with hazard
resolution using interlocks.
• Implemented a discrete event simulator to model latency and instruction execution time.
• Integrated caches to reduce memory latency and imporve the performance.
